1. Monitor is showing the Router when its not the router IP Fix (maybe put "Internet" instead)
2. Bold the Device Name in any output, mainly scan port output
3. The monitor output table has massive space between timestamp and TCP/UDP column, cmon rich
4. Add an "exclude-router" flag to the scan command
5. Remove shortcut for --id in any commands
6. Create mapping for mDNS query names
7. Add (Primary) to "en0" interface when there are options for the interface to monitor